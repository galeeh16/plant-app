import { Container } from "react-bootstrap";

const Blog = () => {
	return (
		<Container>
			<h3>Blog</h3>
			<button className="btn btn-primary">Button C</button>
		</Container>
	);
};

export default Blog;
